'use server'

import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'
import { createAdminClient } from '@/lib/supabase/admin'
import { resolveSessionAccess } from '@/lib/auth/access'
import { requireFeature } from '@/lib/billing/entitlements'
import type { Json, TablesInsert, TablesUpdate } from '@/lib/supabase-types'

const IMAGE_TYPES = new Set(['image/jpeg', 'image/png', 'image/webp'])
const MAX_IMAGE_BYTES = 8 * 1024 * 1024
const MAX_GALLERY_FILES = 12
const DEFAULT_TICKET_NAME = 'Bilhete geral'

function text(formData: FormData, key: string, max: number) { return String(formData.get(key) || '').trim().slice(0, max) }
function optionalNumber(formData: FormData, key: string) { const raw=text(formData,key,30); if(!raw)return null; const value=Number(raw); if(!Number.isFinite(value)||value<0)throw new Error(`Valor inválido em ${key}.`); return value }
function validateDate(value:string,label:string){const date=new Date(value);if(!value||Number.isNaN(date.getTime()))throw new Error(`${label} inválida.`);return date}
function manualApprovalEnabled(settings:Json|null|undefined){if(!settings||typeof settings!=='object'||Array.isArray(settings))return true;const eventValue=settings.manual_event_approval;if(typeof eventValue==='boolean')return eventValue;const legacyValue=settings.manual_profile_approval;return typeof legacyValue==='boolean'?legacyValue:true}

async function geocodeAddress(address:string){const value=address.trim();if(value.length<4)return null;try{const url=new URL('https://nominatim.openstreetmap.org/search');url.searchParams.set('q',value);url.searchParams.set('format','jsonv2');url.searchParams.set('limit','1');url.searchParams.set('countrycodes','pt,es');const response=await fetch(url,{headers:{'User-Agent':'Find4Sport/1.0','Accept-Language':'pt-PT,pt;q=0.9'},cache:'no-store',signal:AbortSignal.timeout(5000)});if(!response.ok)return null;const rows=await response.json() as Array<{lat?:string;lon?:string}>;const latitude=Number(rows?.[0]?.lat);const longitude=Number(rows?.[0]?.lon);return Number.isFinite(latitude)&&Number.isFinite(longitude)?{latitude,longitude}:null}catch{return null}}
async function uploadImage(admin:ReturnType<typeof createAdminClient>,userId:string,eventFolderId:string,file:File,prefix:string){if(!IMAGE_TYPES.has(file.type))throw new Error('As imagens devem ser JPEG, PNG ou WebP.');if(file.size>MAX_IMAGE_BYTES)throw new Error('Cada imagem pode ter no máximo 8 MB.');const extension=file.type==='image/png'?'png':file.type==='image/webp'?'webp':'jpg';const path=`${userId}/${eventFolderId}/${prefix}-${crypto.randomUUID()}.${extension}`;const{error}=await admin.storage.from('events').upload(path,file,{contentType:file.type,cacheControl:'3600',upsert:false});if(error)throw new Error(error.message);const{data}=admin.storage.from('events').getPublicUrl(path);return{path,url:data.publicUrl}}
function storagePathForEvent(url:string,userId:string,eventFolderId:string){const marker='/storage/v1/object/public/events/';const path=url.split(marker)[1];return path?.startsWith(`${userId}/${eventFolderId}/`)?path:null}
function validateEventFields(formData:FormData){const title=text(formData,'title',180);if(!title)throw new Error('O título é obrigatório.');const description=text(formData,'description',5000)||null;const categoryId=text(formData,'category_id',60)||null;const address=text(formData,'address',300)||null;const startDate=validateDate(text(formData,'start_date',80),'Data de início');const endRaw=text(formData,'end_date',80);const endDate=endRaw?validateDate(endRaw,'Data de fim'):null;if(endDate&&endDate<=startDate)throw new Error('A data de fim tem de ser posterior à data de início.');const capacity=optionalNumber(formData,'capacity');if(capacity!==null&&(!Number.isInteger(capacity)||capacity<1||capacity>100000))throw new Error('Capacidade inválida.');const priceMin=optionalNumber(formData,'price_min');const priceMax=optionalNumber(formData,'price_max');if(priceMin!==null&&priceMax!==null&&priceMax<priceMin)throw new Error('O preço máximo não pode ser inferior ao preço mínimo.');return{title,description,categoryId,address,startDate,endDate,capacity,priceMin,priceMax}}
async function validateCategory(admin:ReturnType<typeof createAdminClient>,categoryId:string|null){if(!categoryId)return;const{data:category}=await admin.from('categories').select('id').eq('id',categoryId).maybeSingle();if(!category)throw new Error('Modalidade inválida.')}
async function syncDefaultTicket(admin:ReturnType<typeof createAdminClient>,eventId:string,priceMin:number|null,capacity:number|null){const{data:tickets,error:ticketsError}=await admin.from('event_ticket_types').select('id,name,sort_order,is_active').eq('event_id',eventId).eq('is_active',true).order('sort_order',{ascending:true});if(ticketsError)throw new Error(`Não foi possível validar os bilhetes do evento: ${ticketsError.message}`);const activeTickets=tickets||[];const autoTicket=activeTickets.length===1&&activeTickets[0].name===DEFAULT_TICKET_NAME&&Number(activeTickets[0].sort_order||0)===0?activeTickets[0]:null;const defaultPrice=priceMin??0;const defaultCapacity=capacity??0;if(activeTickets.length===0&&defaultPrice>0){const payload:TablesInsert<'event_ticket_types'>={event_id:eventId,name:DEFAULT_TICKET_NAME,description:'Acesso geral ao evento',price:defaultPrice,capacity:defaultCapacity,is_active:true,sort_order:0};const{error}=await admin.from('event_ticket_types').insert(payload);if(error)throw new Error(`Não foi possível criar o bilhete do evento: ${error.message}`);return}if(autoTicket){if(defaultPrice>0){const{error}=await admin.from('event_ticket_types').update({price:defaultPrice,capacity:defaultCapacity}).eq('id',autoTicket.id);if(error)throw new Error(`Não foi possível atualizar o bilhete do evento: ${error.message}`)}else{const{error}=await admin.from('event_ticket_types').update({is_active:false}).eq('id',autoTicket.id);if(error)throw new Error(`Não foi possível desativar o bilhete do evento: ${error.message}`)}}}

export async function createEventAction(formData:FormData){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)throw new Error('Autenticação necessária.');const access=await resolveSessionAccess(supabase,user);if(!access||!['professional','venue_manager'].includes(access.role))throw new Error('Apenas profissionais e gestores de espaço podem criar eventos.');await requireFeature(user.id,'events.create.enabled');const fields=validateEventFields(formData);const admin=createAdminClient();await validateCategory(admin,fields.categoryId);const coordinates=fields.address?await geocodeAddress(fields.address):null;let organizerName=user.user_metadata?.full_name||'Organizador';let professionalId:string|null=null;if(access.role==='professional'){const{data:professional}=await admin.from('professionals').select('id,full_name,professional_name,status').eq('user_id',user.id).maybeSingle();if(!professional)throw new Error('Perfil profissional não encontrado.');if(professional.status!=='active')throw new Error('O perfil profissional tem de estar ativo para criar eventos.');professionalId=professional.id;organizerName=professional.professional_name||professional.full_name||organizerName}else{const{data:space}=await admin.from('sport_spaces').select('id,name,status').eq('owner_user_id',user.id).order('created_at',{ascending:true}).limit(1).maybeSingle();if(!space)throw new Error('Não existe um espaço associado a esta conta.');organizerName=space.name||organizerName}const{data:configData}=await admin.from('system_config').select('settings').maybeSingle();const status:NonNullable<TablesInsert<'events'>['status']>=manualApprovalEnabled(configData?.settings)?'pending':'published';const banner=formData.get('banner');const gallery=formData.getAll('gallery').filter((value):value is File=>value instanceof File&&value.size>0);if(gallery.length>MAX_GALLERY_FILES)throw new Error(`A galeria pode ter no máximo ${MAX_GALLERY_FILES} imagens.`);const eventId=crypto.randomUUID();const uploaded:Array<{path:string;url:string}>=[];let eventCreated=false;try{let imageUrl:string|null=null;if(banner instanceof File&&banner.size>0){const item=await uploadImage(admin,user.id,eventId,banner,'banner');uploaded.push(item);imageUrl=item.url}for(const file of gallery)uploaded.push(await uploadImage(admin,user.id,eventId,file,'gallery'));const galleryUrls=uploaded.filter(item=>item.url!==imageUrl).map(item=>item.url);const payload:TablesInsert<'events'>={id:eventId,title:fields.title,description:fields.description,category_id:fields.categoryId,address:fields.address,start_date:fields.startDate.toISOString(),end_date:fields.endDate?.toISOString()||null,capacity:fields.capacity,price_min:fields.priceMin,price_max:fields.priceMax,image_url:imageUrl,gallery_urls:galleryUrls.length?galleryUrls:null,created_by:user.id,professional_id:professionalId,organizer_name:organizerName,status,latitude:coordinates?.latitude??null,longitude:coordinates?.longitude??null};const{error}=await admin.from('events').insert(payload);if(error)throw new Error(error.message||'Não foi possível criar o evento.');eventCreated=true;await syncDefaultTicket(admin,eventId,fields.priceMin,fields.capacity);revalidatePath('/dashboard/eventos');revalidatePath('/eventos');revalidatePath('/pesquisa');return{success:true,eventId,status}}catch(error){if(eventCreated)await admin.from('events').delete().eq('id',eventId);if(uploaded.length)await admin.storage.from('events').remove(uploaded.map(item=>item.path));throw error}}

export async function updateEventAction(eventId:string,formData:FormData){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)throw new Error('Autenticação necessária.');const access=await resolveSessionAccess(supabase,user);if(!access||!['professional','venue_manager'].includes(access.role))throw new Error('Sem permissão para editar eventos.');const admin=createAdminClient();const{data:current}=await admin.from('events').select('id,created_by,image_url,gallery_urls,status,address').eq('id',eventId).eq('created_by',user.id).maybeSingle();if(!current)throw new Error('Evento não encontrado ou sem permissão para editar.');const fields=validateEventFields(formData);await validateCategory(admin,fields.categoryId);const addressChanged=String(current.address||'').trim()!==String(fields.address||'').trim();const coordinates=addressChanged&&fields.address?await geocodeAddress(fields.address):null;const currentGallery=Array.isArray(current.gallery_urls)?current.gallery_urls.filter((value):value is string=>typeof value==='string'):[];const requestedExisting=formData.getAll('existing_gallery').map(String);const keptGallery=requestedExisting.filter(url=>currentGallery.includes(url)).slice(0,MAX_GALLERY_FILES);const newGallery=formData.getAll('gallery').filter((value):value is File=>value instanceof File&&value.size>0);if(keptGallery.length+newGallery.length>MAX_GALLERY_FILES)throw new Error(`A galeria pode ter no máximo ${MAX_GALLERY_FILES} imagens.`);const banner=formData.get('banner');const removeBanner=text(formData,'remove_banner',10)==='true';const{data:configData}=await admin.from('system_config').select('settings').maybeSingle();const requiresReapproval=current.status==='published'&&manualApprovalEnabled(configData?.settings);const nextStatus=requiresReapproval?'pending':current.status;const uploaded:Array<{path:string;url:string}>=[];try{let imageUrl=removeBanner?null:current.image_url;if(banner instanceof File&&banner.size>0){const item=await uploadImage(admin,user.id,eventId,banner,'banner');uploaded.push(item);imageUrl=item.url}for(const file of newGallery)uploaded.push(await uploadImage(admin,user.id,eventId,file,'gallery'));const galleryUrls=[...keptGallery,...uploaded.filter(item=>item.url!==imageUrl).map(item=>item.url)];const payload:TablesUpdate<'events'>={title:fields.title,description:fields.description,category_id:fields.categoryId,address:fields.address,start_date:fields.startDate.toISOString(),end_date:fields.endDate?.toISOString()||null,capacity:fields.capacity,price_min:fields.priceMin,price_max:fields.priceMax,image_url:imageUrl,gallery_urls:galleryUrls.length?galleryUrls:null,status:nextStatus,...(addressChanged?{latitude:coordinates?.latitude??null,longitude:coordinates?.longitude??null}:{})};const{error}=await admin.from('events').update(payload).eq('id',eventId).eq('created_by',user.id);if(error)throw new Error(error.message);await syncDefaultTicket(admin,eventId,fields.priceMin,fields.capacity);const removedUrls=currentGallery.filter(url=>!galleryUrls.includes(url));if(current.image_url&&current.image_url!==imageUrl)removedUrls.push(current.image_url);const paths=[...new Set(removedUrls.map(url=>storagePathForEvent(url,user.id,eventId)).filter((value):value is string=>Boolean(value)))];if(paths.length)await admin.storage.from('events').remove(paths);revalidatePath('/dashboard/eventos');revalidatePath(`/dashboard/eventos/${eventId}/editar`);revalidatePath(`/eventos/${eventId}`);revalidatePath('/eventos');revalidatePath('/pesquisa');return{success:true,status:nextStatus,requiresReapproval}}catch(error){if(uploaded.length)await admin.storage.from('events').remove(uploaded.map(item=>item.path));throw error}}
