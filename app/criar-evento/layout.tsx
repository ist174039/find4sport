import PublicPageLayout from '@/components/public-page-layout'

export default function CriarEventoLayout({ children }: { children: React.ReactNode }) {
  return <PublicPageLayout>{children}</PublicPageLayout>
}
