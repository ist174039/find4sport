'use client'

import Link from 'next/link'
import { ArrowRight, Building2, CalendarDays, MapPin, Star, UserRound, Users } from 'lucide-react'
import { Badge } from '@/components/ui/badge'

export type SearchResultListItem = {
  id:string; itemType:'space'|'professional'|'event'|'community'; title:string; subtitle:string; address:string; rating_avg:number|null; review_count:number|null; is_verified:boolean; image_url?:string|null; link:string; distanceKm?:number|null
}

function ResultImage({item}:{item:SearchResultListItem}){const Icon=item.itemType==='space'?Building2:item.itemType==='professional'?UserRound:item.itemType==='event'?CalendarDays:Users;return <div className="relative h-36 w-full shrink-0 overflow-hidden rounded-xl bg-gradient-to-br from-primary/15 via-muted to-secondary/20 sm:h-28 sm:w-28">{item.image_url?<img className="h-full w-full object-cover" alt={item.title} src={item.image_url}/>:<div className="flex h-full items-center justify-center"><Icon className="h-9 w-9 text-primary/35"/></div>}</div>}
function label(item:SearchResultListItem){return item.itemType==='space'?'Espaço':item.itemType==='professional'?'Profissional':item.itemType==='event'?'Evento':'Comunidade'}

export function SearchResultsList({items}:{items:SearchResultListItem[]}){
  function focus(item:SearchResultListItem){window.dispatchEvent(new CustomEvent('f4s-show-map'));requestAnimationFrame(()=>window.dispatchEvent(new CustomEvent('f4s-map-focus',{detail:{id:item.id}})))}
  return <div className="space-y-3">{items.map(item=><article key={item.id} role="button" tabIndex={0} onClick={()=>focus(item)} onKeyDown={event=>{if(event.key==='Enter'||event.key===' '){event.preventDefault();focus(item)}}} className="group flex cursor-pointer flex-col gap-3 rounded-2xl border border-border bg-card p-3 transition hover:border-primary/50 hover:shadow-sm focus:outline-none focus:ring-2 focus:ring-primary/30 sm:flex-row sm:p-4"><ResultImage item={item}/><div className="flex min-w-0 flex-1 flex-col"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><div className="mb-1 flex flex-wrap gap-1.5"><Badge variant="outline" className="text-[10px]">{label(item)}</Badge>{item.is_verified&&<Badge variant="success" className="text-[10px]">Verificado</Badge>}</div><h2 className="line-clamp-1 font-semibold group-hover:text-primary">{item.title}</h2></div>{item.rating_avg!==null&&Number(item.review_count||0)>0&&<span className="flex shrink-0 items-center gap-1 rounded-lg bg-amber-500/10 px-2 py-1 text-xs font-semibold text-amber-700"><Star className="h-3 w-3 fill-amber-500 text-amber-500"/>{Number(item.rating_avg).toFixed(1)}</span>}</div><p className="mt-2 line-clamp-2 text-sm text-muted-foreground">{item.subtitle}</p>{item.address&&<p className="mt-2 flex items-center gap-1.5 text-xs text-muted-foreground"><MapPin className="h-3.5 w-3.5 shrink-0 text-primary"/><span className="truncate">{item.address}</span></p>}{item.distanceKm!=null&&<p className="mt-1 text-xs font-medium text-primary">{item.distanceKm<1?`${Math.round(item.distanceKm*1000)} m`:`${item.distanceKm.toFixed(1)} km`} de ti</p>}<div className="mt-auto flex items-center justify-between gap-3 pt-3"><span className="text-[11px] text-muted-foreground">Toca no cartão para localizar no mapa</span><Link href={item.link} onClick={event=>event.stopPropagation()} className="inline-flex min-h-9 shrink-0 items-center rounded-lg bg-primary/10 px-3 text-xs font-semibold text-primary hover:bg-primary/15">Ver detalhe<ArrowRight className="ml-1 h-3.5 w-3.5"/></Link></div></div></article>)}</div>
}
